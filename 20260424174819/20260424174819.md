# Golang Interview Questions

1. Buffered vs unbuffered: what blocks, when, and how it changes backpressure. 
2. `select`: what happens with multiple ready cases, `default`, and `nil` channels. 
3. Goroutine leaks: show me the code path that blocks forever on send/recv. 
4. Context cancellation: where do you check `ctx.Done()` and how do you make send/recv cancellable.

#golang #interviews 